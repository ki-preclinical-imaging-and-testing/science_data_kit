#!/usr/bin/env python
"""
Science Data Kit - Legacy App Runner

This script previously ran the legacy version of the Science Data Kit application.
The legacy app has been removed from the codebase. The code can still be accessed
from the git history if needed for reference.

For the current version, use run_app.py instead.
"""
import sys

if __name__ == "__main__":
    print("The legacy app has been removed from the codebase.")
    print("The code can still be accessed from the git history if needed for reference.")
    print("For the current version, use run_app.py instead.")
    sys.exit(1)
